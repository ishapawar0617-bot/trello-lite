services:
  backend:
    build: ./backend
    container_name: trello-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: trello-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend